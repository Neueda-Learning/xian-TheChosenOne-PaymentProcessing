<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tco.safepay.mapper.BalanceLedgerMapper">

    <resultMap id="BalanceLedgerResultMap" type="org.tco.safepay.model.entity.BalanceLedger">
        <id column="id" property="id" javaType="java.util.UUID" jdbcType="VARCHAR" typeHandler="org.tco.safepay.config.typehandler.UuidTypeHandler"/>
        <result column="account_no" property="accountNo"/>
        <result column="payment_id" property="paymentId" javaType="java.util.UUID" jdbcType="VARCHAR" typeHandler="org.tco.safepay.config.typehandler.UuidTypeHandler"/>
        <result column="direction" property="direction"/>
        <result column="amount" property="amount"/>
        <result column="balance_before" property="balanceBefore"/>
        <result column="balance_after" property="balanceAfter"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="org.tco.safepay.model.entity.BalanceLedger">
        INSERT INTO balance_ledger (
        id,
        account_no,
        payment_id,
        direction,
        amount,
        balance_before,
        balance_after,
        created_at
        ) VALUES (
        #{id,jdbcType=VARCHAR,typeHandler=org.tco.safepay.config.typehandler.UuidTypeHandler},
        #{accountNo},
        #{paymentId,jdbcType=VARCHAR,typeHandler=org.tco.safepay.config.typehandler.UuidTypeHandler},
        #{direction},
        #{amount},
        #{balanceBefore},
        #{balanceAfter},
        #{createdAt}
        )
    </insert>

    <select id="selectById" resultMap="BalanceLedgerResultMap">
        SELECT id, account_no, payment_id, direction, amount, balance_before, balance_after, created_at
        FROM balance_ledger
        WHERE id = #{id,jdbcType=VARCHAR,typeHandler=org.tco.safepay.config.typehandler.UuidTypeHandler}
    </select>

    <select id="selectByAccountNo" resultMap="BalanceLedgerResultMap">
        SELECT id, account_no, payment_id, direction, amount, balance_before, balance_after, created_at
        FROM balance_ledger
        WHERE account_no = #{accountNo}
        ORDER BY created_at DESC
    </select>

    <select id="selectByPaymentId" resultMap="BalanceLedgerResultMap">
        SELECT id, account_no, payment_id, direction, amount, balance_before, balance_after, created_at
        FROM balance_ledger
        WHERE payment_id = #{paymentId,jdbcType=VARCHAR,typeHandler=org.tco.safepay.config.typehandler.UuidTypeHandler}
        ORDER BY created_at DESC
    </select>

</mapper>
